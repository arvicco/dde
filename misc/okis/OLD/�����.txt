Приглашаем принять участие программистов и продвинутых пользователей в тестировании DDE сервера
Теперь вы можете сами создавать DDE сервер(а) и робота(ов) на удобном вам языке Delphi,VB,VBS,JS,PERL,Python,Rexx,Net
для любых таблиц, параллельно анализировать получаемые данные, хранить в любом источнике данных, используя ADO,ODBC,Индексы

Прилагаются примеры на VBS для экспорта данных в акс базу онлайн

К лету планируется ActiveX сервер заявок, к осени- удаленное управление ДДЕ через интернет в онлайн
Возможно сотрудничество

Запрос на legkome@mail.ru


Важно!!! dde.exe будет работать до 01.01.2010
это не окончательный релиз, в нем могут быть ошибки.
Все скрипты (vbs,wsc) даются для примера, Вы можете их
менять давать другим итд.
Так же это относится и к FINDHTA68.exe

вопросы можно legkome@mail.ru

Если у вас виста то надо поменять настройки в регистре 
здесь
HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Jet\4.0\Engines\Text
"Format"="Delimited(;)"
или запустить файл jet40.reg


1. Создать ПАПКУ (пусть SERVER) в том месте где лежит info.exe
 
2. Разархивировать туда архив
   (   Там есть файл БыстрыйСтарт.vbs. 
   Это файл для примера, запустите его (2 раза мышкой). Откроется
   консольное окно, не закрывая его откройет квик, настройте таблицу(-ы)
   для вывода по ДДЕ (DDE Server=excel). В окне будут отображаться
   данные из таблиц квика. )

3. Подключиться к БД. Открыть(2 раза мышкой) файл ..\SERVER\QUIK.UDL
   На вкладке Подключение в 1 выбираем бд ..\SERVER\QUIK.MDB
   [Проверить подключение]
   [OK]
4. В блокноте (notepad.exe) открыть START.VBS
   прочитать комменты (начинаются с ['])

5. В блокноте (notepad.exe) открыть DDE.VBS
   прочитать комменты (начинаются с ['])

6. В блокноте (notepad.exe) открыть DDE.WSC
   прочитать комменты (начинаются с ['])

7. В блокноте (notepad.exe) открыть ANALYS.WSC
   прочитать комменты (начинаются с ['])
8. Настройте БД. Для этого открыть файл quik.udl (2 раза мышкой)
   Если вы хотите работать с акс переходите на вкладку 2(Connection)
   нажмите [...] и выберите файл quik.mdb. 
   Если MS SQL сервер выберите провайдера на вкладке провайдер
   и далее Connection заполните необходимые поля
   
   [Test Connection] и [OK]

8. Запустить START.VBS (2 раза мышк)
   Программа создаст архив, запустит дде сервера, запустит квик,
   настройте в квикe таблицы в дде (см в start.vbs комментарии)
9.  В каталоге ..\SERVER\ARXIV\2009\MM\YY будут файлы csv это таблицы.
    Откройте их, посмотрите что к чему
   Теперь на основе их можно создать таблицы в БД
   
10 Запустить CSV2SQL.VBS (2 раза мышк) выберите папку где лежат файлы csv.
   Программа сможет создать таблицы только если там больше 2х строк.  

11 Повторите п 8 теперь данные будут писаться уже в БД. 
   (см dde.vbs)



GearBox.qpl - кпайл скрипт который рисует метки и выполняет кпайл команды из файла
GearBox.wsc - com для работы с метками и командами кпайл
ML_EXAMPLE.vbs - пример работы GearBox.wsc

TEST.vbs - для запуска и отладки роботов и стратегий в тестовом режиме
           (эмуляция прихода тиков, эмуляция dde.exe)
ANALYS.vbs - скрипт для тестового запуска ANALYS.WSC
           (скрипт приемник тиков и запуск анализа)  


